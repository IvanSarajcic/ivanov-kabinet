КОНТРОЛНИ ЗАДАТАК — Алгоритми и коректност (C++)
Укупно: 10 питања (50 поена)

– 5 питања са кратким одговором (5 × 4 = 20 поена)
– 5 питања вишеструког избора (5 × 6 = 30 поена)

I. Питања са кратким одговором (4 поена по задатку)
1. Дефиниши алгоритам.

Одговор: _________________________________________________

2. Шта значи да је алгоритам коректан?

Одговор: _________________________________________________

3. Размотри функцију која треба да врати минимум три броја:
int Min(int a, int b, int c) {
    if (a < b && a < c) return a;
    else if (b < c) return b;
    else return c;
}


Дај један пример вредности (a, b, c) за које функција НЕ ради исправно.
Одговор: _________________________________________________

4. Дат је низ:

a = {5, 3, 2, 1}

Добија се низ префиксних збирова:

b[0] = a[0];
for (int i = 1; i < 4; i++)
    b[i] = b[i-1] + a[i];


Упиши добијени низ b = { ?, ?, ?, ? }.
Одговор: _________________________________________________

5. Шта је инваријанта петље и зашто је важна?

Одговор: _________________________________________________

II. Питања вишеструког избора (6 поена по задатку)

Заокружити један тачан одговор.

6. Шта најбоље описује алгоритам?

A. Код који је већ компајлиран
B. Тачан и коначан поступак за решавање проблема
C. Скуп наредби које се извршавају насумично
D. Низ случајних инструкција

7. Размотри следећи код:
int n = a.size();
vector<int> b(n);
b[0] = a[0];
for (int i = 1; i < n - 1; i++)
    b[i + 1] = b[i] + a[i + 1];


A. Програм увек пада при извршавању
B. Може да падне, зависи од дужине низа
C. Завршиће рад, али b често неће бити исправан
D. Радиће исправно за сваки непразан низ

8. Шта ће се догодити са следећим кодом?
for (int i = 1; i < n++; i++)
    s += i;


A. Програм увек пада
B. Ради бесконачно
C. Завршиће, али резултат НЕЋЕ бити исправан
D. Радиће исправно

**9. Функција исписује три броја од најмањег до највећег.

Тест примери:**

A: (1, 2, 3)
B: (15, 23, 18)
V: (2, 5, 3)
G: (4, 2, 7)
D: (17, 30, 12)
Đ: (15, 12, 18)
E: (52, 45, 31)

A. Тест A је сувише једноставан
B. Тест V проверава исто што и B
C. Свака неисправна функција биће ухваћена бар једним тестом
D. Ништа од понуђеног

10. Шта је главна грешка у овом алгоритму?
for (int i = 0; i < n; i++)
    b[i + 1] = b[i] + a[i + 1];


A. Петља се никад не извршава
B. Индекс излази ван граница низа
C. Збир није исправан јер се користи +=
D. Алгоритам увек ради исправно