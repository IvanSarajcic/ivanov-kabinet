# КОНТРОЛНИ ЗАДАТАК 1 — Алгоритми и коректност (C++)
**Име и презиме:** _________________________________________________  
**Разред и одељење:** __________________  
**Датум:** __________________

---

### Упутство
*   Тест има укупно **10 питања**.
*   Максималан број поена је **50**.
*   Пажљиво прочитајте свако питање пре него што одговорите.
*   Пишите читко.

---

### I. Питања са кратким одговором (4 поена по задатку)

**1. Дефиниши алгоритам.**  
Одговор: _________________________________________________________________________  
__________________________________________________________________________________

**2. Шта значи да је алгоритам парцијално коректан?**  
Одговор: _________________________________________________________________________  
__________________________________________________________________________________

**3. Размотри функцију која треба да врати минимум три броја:**
```cpp
int Min(int a, int b, int c) {
    if (a < b && a < c) return a;
    else if (b < c) return b;
    else return c;
}
```
Дај један пример вредности (a, b, c) за које функција **НЕ** ради исправно.  
Одговор: (a, b, c) = (____, ____, ____)

**4. Дат је низ `a = {5, 3, 2, 1}`. Израчунава се низ префиксних збирова `b`:**
```cpp
b[0] = a[0];
for (int i = 1; i < 4; i++)
    b[i] = b[i-1] + a[i];
```
Упиши елементе добијеног низа `b`.  
Одговор: b = { ____, ____, ____, ____ }

**5. Шта је "грешка за 1" (off-by-one error)?**  
Одговор: _________________________________________________________________________  
__________________________________________________________________________________

---

### II. Питања вишеструког избора (6 поена по задатку)
*Заокружи један тачан одговор.*

**6. Који од наведених исказа најбоље описује тоталну коректност алгоритма?**  
A. Алгоритам је парцијално коректан и зауставља се за све исправне улазе.  
B. Алгоритам ради брзо и не заузима пуно меморије.  
C. Алгоритам је написан у C++ језику без синтаксних грешака.  
D. Алгоритам даје тачан резултат за већину улаза.

**7. Размотри следећи код:**
```cpp
int n = a.size();
vector<int> b(n);
b[0] = a[0];
for (int i = 1; i < n - 1; i++)
    b[i + 1] = b[i] + a[i + 1];
```
Шта је проблем са овим кодом ако је циљ да се попуни цео низ `b`?  
A. Програм ће се срушити због дељења нулом.  
B. Петља се не извршава довољан број пута (последњи елемент неће бити израчунат).  
C. Индекс излази ван граница низа.  
D. Код је потпуно исправан.

**8. Шта ће се догодити са следећим кодом ако је `n` велики позитиван број?**
```cpp
for (int i = 1; i < n; i++)
    s += i; // s је типа int
```
A. Програм ће ући у бесконачну петљу.  
B. Може доћи до прекорачења (overflow) ако збир постане превелик за тип `int`.  
C. Програм ће увек исписати 0.  
D. Ништа од наведеног.

**9. Функција треба да сортира три броја. Тестирамо је на улазима:**
*   A: (1, 2, 3)
*   B: (3, 2, 1)
*   C: (1, 3, 2)
*   D: (2, 1, 3)

Зашто није довољно тестирати само случај А?  
A. Зато што је то већ сортиран низ, па не проверава логику замене места.  
B. Зато што су бројеви премали.  
C. Зато што C++ не ради добро са малим бројевима.  
D. Довољно је, ако ради за А, радиће за све.

**10. Која врста верификације подразумева математички доказ исправности без покретања кода?**  
A. Динамичка верификација.  
B. Статичка верификација.  
C. Јединично тестирање (Unit testing).  
D. Бета тестирање.
