# КОНТРОЛНИ ЗАДАТАК 1 — Алгоритми и коректност (C++) — ЗА НАСТАВНИКА
**РЕШЕЊА И БОДОВАЊЕ**

---

### БОДОВАЊЕ
*   **Питања 1–5 (Кратки одговори):** 4 поена свако (Укупно 20 поена)
*   **Питања 6–10 (Вишеструки избор):** 6 поена свако (Укупно 30 поена)
*   **МАКСИМУМ:** 50 поена

**Предлог скале за оцењивање:**
*   45–50 поена: **Одличан (5)**
*   35–44 поена: **Врло добар (4)**
*   25–34 поена: **Добар (3)**
*   15–24 поена: **Довољан (2)**
*   0–14 поена: **Недовољан (1)**

---

### РЕШЕЊА

#### I. Питања са кратким одговором

**1. Дефиниши алгоритам.**  
*Одговор:* Алгоритам је прецизан опис поступка за решавање неког проблема у коначном броју корака. (Прихватити и сличне дефиниције које помињу коначност, прецизност и решавање проблема).

**2. Шта значи да је алгоритам парцијално коректан?**  
*Одговор:* Значи да ако се алгоритам заустави, резултат који врати је тачан (задовољава спецификацију). Не гарантује да ће се зауставити.

**3. Пример за који функција Min НЕ ради исправно:**

*Тачан одговор:* `(3, 1, 1)` или `(1, 3, 1)` или било која тројка где минимум није на првој позицији и где су друга два броја једнака.

*Објашњење:*  
За улаз `(3, 1, 1)`:
- Први услов `a < b && a < c` → `3 < 1 && 3 < 1` → `false`
- Други услов `b < c` → `1 < 1` → `false`
- Враћа `c = 1` (случајно тачно)

За улаз `(1, 3, 1)`:
- Први услов `a < b && a < c` → `1 < 3 && 1 < 1` → `false` (због `1 < 1`)
- Други услов `b < c` → `3 < 1` → `false`
- Враћа `c = 1` (случајно тачно)

**Главни проблем:** Функција не покрива случајеве где је минимум једнак са другим вредностима због употребе строгог поређења (`<`) уместо `<=`. Иако често враћа тачну вредност, логика није потпуна и може грешити у другим ситуацијама.

**4. Префиксни збирови за {5, 3, 2, 1}**  
*Одговор:* `b = {5, 8, 10, 11}`.

**5. Шта је "грешка за 1"?**  
*Одговор:* Грешка која настаје када се петља изврши један пут превише или један пут премало, или када се приступи индексу који је за један већи/мањи од дозвољеног.

#### II. Питања вишеструког избора

**6. А** (Алгоритам је парцијално коректан и зауставља се за све исправне улазе)

**7. B** (Петља се не извршава довољан број пута)

*Објашњење:* Петља иде од `i = 1` до `i < n - 1`. За низ величине `n = 4`, индекс `i` иде само 1 и 2. Постављају се `b[2]` и `b[3]`, али `b[1]` се прескаче и остаје неиницијализован. Последња вредност `b[n-1]` јесте постављена, али средње вредности нису комплетне.

**8. B** (Може доћи до прекорачења (overflow) ако збир постане превелик за тип int)

**9. A** (Зато што је то већ сортиран низ, па не проверава логику замене места)

**10. B** (Статичка верификација)
